<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.mirrorer.lijian">

    <!-- 用于写入缓存数据到扩展存储卡 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_colck" />
        <activity android:name=".Activity_hello" />

        <service
            android:name=".ClockService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ScreenActivity" />
        <activity android:name=".NewplanActivity" />
        <activity android:name=".HOMEActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".ClockActivity" />
        <activity android:name=".exchangeClockActivity" />
        <activity android:name=".NewclockActivity" />
        <activity android:name=".ATestActivity"></activity>
    </application>

</manifest>